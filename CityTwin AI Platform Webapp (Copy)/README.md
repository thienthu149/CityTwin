
  # CityTwin AI Platform Webapp (Copy)

  This is a code bundle for CityTwin AI Platform Webapp (Copy). The original project is available at https://www.figma.com/design/LER6DKIJTrxyOYo2bhjmcA/CityTwin-AI-Platform-Webapp--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  